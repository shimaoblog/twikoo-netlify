[build]
  functions = "netlify/functions"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

# 关键！把 /api/* 的请求转发给 Twikoo 函数
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/twikoo/:splat"
  status = 200
